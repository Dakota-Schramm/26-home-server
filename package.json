{
  "name": "home-server",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "dev:mailer": "npm run dev --workspace=packages/mailer",
    "dev:scraper": "npm run dev --workspace=packages/scraper"
  }
}
